#include <stdio.h>

// 문제: while문을 사용해서 1~100까지의 값을 홀수합, 짝수합으로 계산하세요.

int main() {
    int i, n;
    int evenSum = 0; 
    int oddSum = 0;

    while(1<=n<=100) {
        if(n%2==0) {
            evenSum += n;
        }
        else {
            
        }
        
        i += 1;
    }
}